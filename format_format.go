package python

// File automatically generated with format.sh.

import "fmt"

// Format implements the fmt.Formatter interface
func (f AddExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AddExpression
		type AddExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AndExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AndExpression
		type AndExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AndTest) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AndTest
		type AndTest X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AnnotatedAssignmentStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AnnotatedAssignmentStatement
		type AnnotatedAssignmentStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ArgumentList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ArgumentList
		type ArgumentList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ArgumentListOrComprehension) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ArgumentListOrComprehension
		type ArgumentListOrComprehension X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AssertStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AssertStatement
		type AssertStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AssignmentExpressionAndSuite) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AssignmentExpressionAndSuite
		type AssignmentExpressionAndSuite X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AssignmentExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AssignmentExpression
		type AssignmentExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AssignmentStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AssignmentStatement
		type AssignmentStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Atom) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Atom
		type Atom X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AugmentedAssignmentStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AugmentedAssignmentStatement
		type AugmentedAssignmentStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f AugTarget) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = AugTarget
		type AugTarget X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ClassDefinition) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ClassDefinition
		type ClassDefinition X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Comparison) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Comparison
		type Comparison X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ComparisonExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ComparisonExpression
		type ComparisonExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f CompoundStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = CompoundStatement
		type CompoundStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Comprehension) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Comprehension
		type Comprehension X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ComprehensionFor) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ComprehensionFor
		type ComprehensionFor X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ComprehensionIf) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ComprehensionIf
		type ComprehensionIf X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ComprehensionIterator) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ComprehensionIterator
		type ComprehensionIterator X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ConditionalExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ConditionalExpression
		type ConditionalExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Decorators) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Decorators
		type Decorators X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f DefParameter) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = DefParameter
		type DefParameter X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f DelStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = DelStatement
		type DelStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f DictDisplay) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = DictDisplay
		type DictDisplay X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f DictItem) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = DictItem
		type DictItem X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Enclosure) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Enclosure
		type Enclosure X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Except) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Except
		type Except X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Expression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Expression
		type Expression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ExpressionList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ExpressionList
		type ExpressionList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f File) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = File
		type File X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ForStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ForStatement
		type ForStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f FuncDefinition) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = FuncDefinition
		type FuncDefinition X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f GeneratorExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = GeneratorExpression
		type GeneratorExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f GlobalStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = GlobalStatement
		type GlobalStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f IfStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = IfStatement
		type IfStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ImportStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ImportStatement
		type ImportStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f KeywordArgument) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = KeywordArgument
		type KeywordArgument X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f KeywordItem) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = KeywordItem
		type KeywordItem X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f LambdaExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = LambdaExpression
		type LambdaExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ModuleAs) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ModuleAs
		type ModuleAs X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Module) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Module
		type Module X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f MultiplyExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = MultiplyExpression
		type MultiplyExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f NonLocalStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = NonLocalStatement
		type NonLocalStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f NotTest) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = NotTest
		type NotTest X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f OrExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = OrExpression
		type OrExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f OrTest) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = OrTest
		type OrTest X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Parameter) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Parameter
		type Parameter X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ParameterList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ParameterList
		type ParameterList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f PositionalArgument) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = PositionalArgument
		type PositionalArgument X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f PowerExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = PowerExpression
		type PowerExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f PrimaryExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = PrimaryExpression
		type PrimaryExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f RaiseStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = RaiseStatement
		type RaiseStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f RelativeModule) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = RelativeModule
		type RelativeModule X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ReturnStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ReturnStatement
		type ReturnStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f ShiftExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = ShiftExpression
		type ShiftExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f SimpleStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = SimpleStatement
		type SimpleStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f SliceItem) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = SliceItem
		type SliceItem X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f SliceList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = SliceList
		type SliceList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredExpression
		type StarredExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredExpressionList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredExpressionList
		type StarredExpressionList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredItem) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredItem
		type StarredItem X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredList
		type StarredList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredListOrComprehension) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredListOrComprehension
		type StarredListOrComprehension X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StarredOrKeywordArgument) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StarredOrKeywordArgument
		type StarredOrKeywordArgument X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Statement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Statement
		type Statement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f StatementList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = StatementList
		type StatementList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Suite) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Suite
		type Suite X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f Target) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = Target
		type Target X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f TargetList) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = TargetList
		type TargetList X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f TryStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = TryStatement
		type TryStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f TypeParam) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = TypeParam
		type TypeParam X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f TypeParams) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = TypeParams
		type TypeParams X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f TypeStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = TypeStatement
		type TypeStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f UnaryExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = UnaryExpression
		type UnaryExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f WhileStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = WhileStatement
		type WhileStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f WithItem) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = WithItem
		type WithItem X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f WithStatement) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = WithStatement
		type WithStatement X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f WithStatementContents) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = WithStatementContents
		type WithStatementContents X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f XorExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = XorExpression
		type XorExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}

// Format implements the fmt.Formatter interface
func (f YieldExpression) Format(s fmt.State, v rune) {
	if v == 'v' && s.Flag('#') {
		type X = YieldExpression
		type YieldExpression X

		fmt.Fprintf(s, "%#v", (f))
	} else {
		format(&f, s, v)
	}
}
